<h1>Veggies!</h1>

<h2>Eat your Veggies!</h2>

<ul>
  {{#each veg}}
    {{#unless veg_state}}
      {{> veg/veg-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>So happy you ate these Veggies!</h2>

<ul>
  {{#each veg}}
    {{#if veg_state}}
      {{> veg/veg-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Veggie</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Vegetable Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="devoured">Devoured?</label><br>
    <input type="radio" name="devoured" value="0" checked> Uneaten
    <input type="radio" name="devoured" value="1"> Devoured!<br>
  </div>

  <button type="submit">Add Veggie</button>
</form>
